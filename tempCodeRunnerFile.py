    index = random.randint(0, 9)  # index
    # for j in range(temp, 10):
    #     temp1 = temp1+population[i]
    # for j in range(temp, 10):
    #     temp2 = temp2+population[i+1]

    temp1 = population[i][temp:len(population[i])]
    print(temp1)
    # for j in range(temp, 10):
    #     k = 0
    #     p = population[i]
    #     p[j] = temp2[k]
    #     k = k+1
    # for j in range(temp, 10):
    #     k = 0
    #     population[i+1][j] = temp1[k]
    #     k = k+1